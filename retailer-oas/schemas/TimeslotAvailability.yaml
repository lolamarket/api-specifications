type: array
items:
  type: object
  properties:
    timeslot:
      $ref: './Timeslot.yaml'
    capacity:
      $ref: './Capacity.yaml'
    available:
      type: boolean
      description: Indicates if the user can book this Timeslot.
      example: true