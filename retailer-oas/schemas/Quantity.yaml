description: An object representing the requested quantity and its unit.
properties:
  quantity:
    description: The quantity.
    example: 2.0
    format: float
    type: number
  unit:
    description: The unit of the quantity.
    example: unit
    type: string
required:
  - quantity
  - unit
type: object
